# Production 환경 변수 예제
# 이 파일을 .env.prod로 복사하고 실제 값으로 수정하세요

# 데이터베이스 연결 (운영 DB)
DATABASE_URL=jdbc:postgresql://your-prod-host:5432/onboarding_db
DATABASE_USERNAME=your_prod_username
DATABASE_PASSWORD=your_prod_password

# JWT 설정
JWT_SECRET=your-very-long-and-secure-jwt-secret-key-minimum-256-bits
JWT_EXPIRATION=86400000

# Spring 프로필
SPRING_PROFILES_ACTIVE=prod

# CORS 설정 (프론트엔드 URL)
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com

# 이메일 설정
# 옵션 1: Brevo API 사용
BREVO_API_KEY=your-brevo-api-key

# 옵션 2: SMTP 사용
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_AUTH=true
# MAIL_STARTTLS=true

# 프론트엔드 URL (이메일 링크 생성용)
FRONTEND_URL=https://your-frontend-domain.com

# 파일 업로드 디렉토리
UPLOAD_DIR=./uploads

# AWS S3 (선택사항)
# AWS_S3_ENABLED=false
# AWS_S3_BUCKET_NAME=
# AWS_S3_REGION=ap-northeast-2
# AWS_S3_ACCESS_KEY=
# AWS_S3_SECRET_KEY=
