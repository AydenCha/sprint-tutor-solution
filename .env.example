# Environment Variables Template
# Copy this file to .env and fill in your values
# DO NOT commit .env file to version control

# ============================================
# Database Configuration (예시 값 – 실제 환경에 맞게 수정)
# ============================================
DATABASE_URL=jdbc:postgresql://localhost:5432/onboarding_db   # ← 로컬 개발용 예시
DATABASE_USERNAME=postgres                                    # ← 예시 계정명
DATABASE_PASSWORD=changeme                                    # ← 반드시 변경해서 사용

# ============================================
# JWT Configuration
# ============================================
# IMPORTANT: Change this in production to a secure random string
JWT_SECRET=<YOUR_SECURE_JWT_SECRET>
JWT_EXPIRATION=86400000

# ============================================
# CORS Configuration
# ============================================
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ============================================
# Email Configuration (Development - MailHog)
# ============================================
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=<SENDER_EMAIL>

# ============================================
# Email Configuration (Production - SMTP or Brevo)
# ============================================
# Option 1: SMTP
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=<SMTP_USERNAME>
# MAIL_PASSWORD=<SMTP_APP_PASSWORD>
# MAIL_FROM=<SENDER_EMAIL>

# Option 2: Brevo API
# BREVO_API_KEY=<BREVO_API_KEY>

# ============================================
# Email URLs (Frontend URLs for email links)
# ============================================
EMAIL_VERIFICATION_URL=http://localhost:5173/auth/verify-email
EMAIL_PASSWORD_RESET_URL=http://localhost:5173/auth/reset-password

# ============================================
# File Upload Configuration
# ============================================
UPLOAD_DIR=./uploads          # 로컬 개발 예시 경로
MAX_FILE_SIZE=52428800        # 50MB (환경에 맞게 조정 가능)

# ============================================
# AWS S3 Configuration (Optional - for video storage)
# ============================================
# AWS_S3_BUCKET_NAME=<YOUR_S3_BUCKET_NAME>
# AWS_S3_REGION=ap-northeast-2
# AWS_S3_ACCESS_KEY=<YOUR_AWS_ACCESS_KEY_ID>
# AWS_S3_SECRET_KEY=<YOUR_AWS_SECRET_ACCESS_KEY>

